export enum EngineStatus {
    started = "started",
    drive = "drive",
    stopped = "stopped",
}

export enum Responses {
    success = 200,
    failure = 500,
}
export enum SortType {
    id = "id",
    wins = "wins",
    time = "time",
}

export enum Order {
    asc = "ASC",
    desc = "DESC",
}

export enum Methods {
    PUT = "PUT",
    GET = "GET",
    DELETE = "DELETE",
    PATCH = "PATCH",
    POST = "POST",
}
